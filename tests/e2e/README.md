# e2e

Playwright drives the built Electron app (`pnpm --filter @autoappz/desktop build` first). Uses `AUTOAPPZ_DATA_DIR` for an isolated data directory and (from M3) `AUTOAPPZ_FAKE_MODEL` for deterministic model responses.
