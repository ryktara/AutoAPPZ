# React + Vite app

- `pnpm install`
- `pnpm dev` — start the dev server
- `pnpm build` — typecheck and build to `dist/`
- `pnpm test` — run unit tests

This project is a plain Vite application and has no dependency on the tool that generated it.
