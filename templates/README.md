# templates

Bundled project templates. Available: `react-vite`, `fullstack-postgres` (React + Vite, Hono API, Drizzle migrations, cookie-session auth, notes CRUD on PostgreSQL). Planned: `nextjs`, `saas`, `dashboard`. Written independently in M2; no template files are copied from any reference project.
