# @autoappz/tools

Tool definitions and the permission-checked tool runtime (file read/edit, search, shell, git, runtime). Every tool declares risk and a capability; execution is project-root scoped.
