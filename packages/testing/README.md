# @autoappz/testing

Shared test helpers plus `startFakeModelServer`: a deterministic OpenAI-compatible chat-completions server (SSE streaming, tool calls, usage, HTTP errors, API-key check) driven by scenario fixtures that match the latest user message and advance turn by turn.
